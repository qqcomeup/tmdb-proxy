TMDB_API_KEY=
ADMIN_API_KEY=

# 默认只监听本机，适合接 Nginx/Caddy/Traefik 反代。
BIND_ADDRESS=127.0.0.1
PORT=54321

# 默认按 HTTPS 反代部署；纯本机 HTTP 测试可改成 false。
COOKIE_SECURE=true

# 只有确认服务仅接受可信反代转发时才开启。
TRUST_PROXY=false
